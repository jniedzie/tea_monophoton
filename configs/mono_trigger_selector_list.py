from lbl_paths import processes, skim, input_base_path, output_base_path, input_skim, redirector

samples = processes
sample_path = ""

# input_directory = f"{input_base_path}/{sample_path}/{input_skim}/"
input_directory = f"{input_base_path}/"
output_trees_dir = f"{output_base_path}/{sample_path}/{skim}/"
